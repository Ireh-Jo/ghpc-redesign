# OS
.DS_Store
Thumbs.db

# Editor — Webstorm/IntelliJ
.idea/
*.iml

# Editor — VSCode
.vscode/

# Node (지금은 안 쓰지만 Phase 2 대비)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Env
.env
.env.local
.env.*.local

# Build outputs (Phase 2 대비)
dist/
build/
.next/
out/

# Claude — 개인/머신별 설정만 제외 (팀과 공유할 설정은 추후 .claude/settings.json으로 별도 관리)
.claude/settings.local.json

# Temp
*.log
*.tmp
/tmp/

# 길찾기 원본 도면 (.ai 대용량 — 로컬 보관, 커밋 금지)
prototypes/wayfind/plans/*.ai

# 임시 분석용 (사용자 핑크경로 렌더)
prototypes/wayfind/lib/B1painted.png
prototypes/wayfind/walk/_src/
