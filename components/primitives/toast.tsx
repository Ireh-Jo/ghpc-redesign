/**
 * Toast primitive — sonner 래퍼 (guardrails/00-rules.md DO#4).
 * `<Toaster />`는 루트 레이아웃에 1회 마운트, 호출은 `toast.success()` 등.
 * 스펙: context/components/primitives/toast.md
 */
export { Toaster } from '@/components/ui/sonner';
export { toast } from 'sonner';
