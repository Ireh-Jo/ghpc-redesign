/**
 * Button primitive — shadcn `button` 래퍼. 페이지/컴포넌트는 여기서만 import (ui/ 직접 import 금지,
 * guardrails/00-rules.md DO#4). 비주얼 재조정은 이 파일 한 곳에서.
 * 스펙: context/components/primitives/button.md
 */
export { Button, buttonVariants, type ButtonProps } from '@/components/ui/button';
