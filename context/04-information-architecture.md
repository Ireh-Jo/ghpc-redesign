# 04. Information Architecture (IA)

> 5개 공개 페이지 + 새가족 + 어드민. GNB 구조와 페이지별 섹션 우선순위.
> 시안 단계에서 잠금된 구조를 본 개발에 그대로 승계.

## 전역 GNB

```
[로고] 교회소개 | 예배와 교육 | 목양과 사역 | 교회 활동 | 새가족 [생방송 보기 CTA]
```

- **새가족** 메뉴는 시각적 강조 (다른 색 또는 우측 정렬) — 새신자 진입 최우선
- **생방송 보기** CTA는 헤더 우측 고정 — 주일/수요예배 시간엔 빨간 점 활성 (`context/components/interactive/live-badge.md`)
- 데스크탑: 드롭다운 2뎁스 (`context/components/layout/header.md`)
- 모바일: 햄버거 → 풀스크린 메뉴 (`context/components/layout/mobile-nav.md`)

### GNB 상호작용 — 잠금 (2026-08-12)

검토 화면 `/dev/gnb`(STEP 1·2 · **2026-09-16 삭제됨** — 합의가 끝나 소임을 다했다. 필요하면 git 복구)를 놓고 디자이너(박정민 집사)와 1:1 합의. **다수 의견 수렴을 하지 않고
둘이 결정하는 방식**을 택함 (`church-approval-strategy` — 디자인 결정권은 위임받은 상태).

| 항목 | 결정 | 사유 |
|---|---|---|
| 메가메뉴 펼침 방식 | **B · 올린 대메뉴 하나만 펼침** (사랑의교회식) | 방문자는 이미 5개 중 궁금한 하나를 골라 올린 상태 — 그것만 보여주는 게 읽기 편하고 패널도 깔끔. A(전체 5열 노출)·C(좌측 5개 유지 하이브리드)는 폐기 |
| 대메뉴 클릭 | **① 클릭 불가.** 링크가 아니라 hover/focus로 패널을 여는 컨트롤. 클릭 불가임을 **색으로 구분** | 대메뉴 랜딩 페이지(허브 요약 ②·전체 나열 ③)를 만들지 않음. ③은 모바일에서 13화면까지 길어져 폐기. 하위가 패널에 다 보이므로 도달성 손실 없음 |
| 대메뉴 도달성 보증 | 하위 항목이 전부 해당 페이지의 앵커(`/worship#times` 등)라 1클릭 도달. **새가족만 패널 안에 `새가족 안내 바로가기 →` CTA 유지** | 새신자 동선은 클릭 불가 정책의 예외 없이도 보장돼야 함 |
| 모바일 메뉴 | 햄버거 → **드릴다운 2단**(대메뉴 5개 목록 → 탭하면 그 메뉴 하위만 전체 화면 + `← 전체메뉴`) | 모바일엔 hover가 없어 B안을 그대로 못 씀. "하나만 딱 보여준다"는 B안 성격을 모바일로 옮긴 형태. 아코디언은 『목양과 사역』(L2 2 + L3 11)에서 한 번 펼치면 화면을 꽉 채워 폐기 |
| 모바일 `새가족` 위치 | **맨 아래 유지** (기존 자리) | 디자이너 판단. 대신 highlight 색으로 구분 |

**후속 검토 (디자이너 제안 · 아직 미결):**

> DECISION NEEDED: **메인 화면 중간 "자주 찾는 메뉴" 아이콘 바로가기** (사랑의교회 참고). 모바일에서
> 새가족이 메뉴 맨 아래로 내려간 것을 보완하는 진입점이 될 수 있음 — 넣을지, 어떤 항목 몇 개인지 미정.

> DECISION NEEDED: **사이트 검색** (만나교회 참고). 검색 대상 범위(주보 PDF·설교 영상 제목·페이지 본문)와
> 구현 방식(Supabase full-text vs 정적 인덱스)이 정해져야 착수 가능. 디자이너가 추가 레퍼런스 조사 중.

### GNB → 라우트 매핑

| GNB | 라우트 | 페이지 문서 |
|---|---|---|
| 교회소개 | `/intro` | `context/pages/02-intro.md` |
| 예배와 교육 | `/worship` | `context/pages/03-worship.md` |
| 목양과 사역 | `/care` | `context/pages/04-care.md` |
| 교회 활동 | `/activity` | `context/pages/05-activity.md` |
| 새가족 | `/newcomer` | `context/pages/06-newcomer.md` |
| (로고) | `/` | `context/pages/01-main.md` |

### 2차 목차 승계 완료 (2026-08-23)

`lib/nav-v2.ts` 검토 초안을 **`lib/nav.ts`로 승계하고 초안 파일은 삭제**했다. GNB 데이터 구조가
`children[]` 평면에서 `groups[].items[]` 2단이 됐다 — 메가메뉴 열 = L2 그룹, 모바일은 2단 화면 안 소제목.

- 대메뉴는 **5개 고정. 신설 금지.** 새 항목은 기존 대메뉴 안 그룹/항목으로 흡수한다.
- 뎁스 상한 L3 유지. L4는 페이지 안 탭(예배 실황 6종 · 영상뉴스 3종 · 자료실 4종 · 전도회 남/여).
- 신설 라우트 13종은 `StubPage`로 먼저 깔았다 (메가메뉴 링크 404 방지):
  `/education` `/ministry` `/giving` `/worship/live` `/activity/{weekly,bulletin,news,denomination}`
  `/church-admin{,/notice,/resources,/apply,/reserve}` `/privacy`
- 신설 라우트에는 콘텐츠 이관 전까지 **현행 사이트 대응 URL**(`lib/nav.ts`의 `legacy`)을
  "현재 홈페이지에서 보기" 링크로 노출한다. 이관 완료 시 `legacy` 값을 지운다.
- **새가족 온라인 등록 폼 노출 중단** (2026-08-09 결정의 코드 적용). `/newcomer`의 `overrides`와
  `/care`의 폼 임베드를 주석으로 걷어냈고, 폼 코드·스키마·서버액션은 보존했다. 복구 방법은 각 파일 주석에 있다.

### 현행 사이트 잔여 메뉴 이관 — 기본값 적용 (2026-08-23)

현행 6개 대메뉴를 4+1로 줄이면서 갈 곳이 없던 항목들을, **현행 사이트 전수 분석**(전 하위 페이지 본문·
접근권한 확인)에 근거해 배치했다. 근거·현행 URL 매핑·폐기한 대안은
[`docs/meetings/2026-08-23-현행사이트-메뉴-전수분석.md`](../docs/meetings/2026-08-23-현행사이트-메뉴-전수분석.md).

교회 회신을 기다리지 않고 **기본값으로 적용하고 이의를 접수하는 방식**(`church-approval-strategy`)을 택했다.
사실 확인이 필요한 3건만 질문으로 남긴다(아래).

| 미배치였던 것 | 배치 | 핵심 근거 |
|---|---|---|
| **집사회 · 권사회** | `목양과 사역 > 목양`에 **항목으로 흡수** (`/care#deacons` `/care#kwonsa`) | 4곳 모두 동일 템플릿(설립목적·역할·활동사진)의 조직 소개라 전도회와 층위가 같다. **그것만을 위해 그룹·대메뉴를 새로 만들지 않는다**(2026-08-23 사용자 지시) — 처음 만들었던 `기관` 그룹은 철회하고 `전도회`도 단일 항목으로 되돌렸다(남/여는 섹션 안 탭) |
| **e교회행정 하위 전체** | **`교회소개 > 교회정보`의 한 줄** → `/church-admin` 허브에서 공지사항 · 자료실 · 신청·서식 · 시설 예약으로 갈라짐 | **PPT 슬라이드 4 원안 위치**(2026-08-23 재확인). 한때 `교회 활동 > 행정·신청` 그룹으로 뒀다가 되돌렸다 — 교회 활동이 11줄로 붐비던 원인이었다. 전 항목이 로그인 없이 공개돼 회원 전용 영역은 만들지 않는다 |
| ├ 교회,교단일정 | `교회 활동 > 일정 > 교회 일정`에 **흡수** | 현행이 구글 캘린더 iframe 한 장 — 이관이 아니라 중복 해소 |
| ├ 기관회계보고 | `자료실` 탭으로 **재분류** | 신청 폼이 아니라 회계장부 xlsx 다운로드. 현행이 `신청서` 그룹에 있는 게 오분류 |
| ├ 시설이용신청 (+키즈그라운드) | `/church-admin/reserve` — 예약시스템 진입점 | `context/features/reservation.md` |
| **교구친선리그** | `/activity/news` 내부 탭으로 흡수 (GNB 항목 아님) | 소식이 아니라 풋살 경기 영상 아카이브(2019~2026 누적) |
| 새가족 수료자 명단 | **미해소 유지** | 실명 공개 개인정보 이슈 — 사실 조사로 풀리는 문제가 아님 |

이로써 `/activity` 독립 라우트 목록이 확정된다(2026-07-01 결정의 `DECISION NEEDED` 해소):
**주보 · 공지사항 · 교회소식 · 교우소식 · 교단소식 · 자료실**(게시판형) + **영상뉴스 · 경향의 일주일**(영상
아카이브, 내부 탭). 캘린더는 `/activity` 안 위젯 유지.

> ~~DECISION NEEDED: **청년회 소속**~~ → **2026-09-06 해소.** 교역자 회의가 **교육**으로 확정했다
> (`/education#young-adult` — 잠정 배치가 그대로 확정된 것이라 코드 변경 없음).
> 단 "`목양`에도 함께 두는가"는 **9/20 확정 예정**. 아래 § 교역자 회의 반영 참조.

> DECISION NEEDED: **구역공과**(현행 `예배 > 구역공과`) — 2차 목차 어디에도 없는 누락 후보.
> 계속 게시한다면 `목양과 사역 > 구역모임` 페이지 내 자료로 흡수하는 것이 자연스럽다. 대상(구역장 전용 여부) 확인 필요.

> DECISION NEEDED: **키즈그라운드 이용신청**(현행 GNB 밖 별도 폼)을 시설이용신청과 통합해도 되는지.

### 교역자 회의 반영 (2026-09-06) — 검토 완료, 회신 대기

2026-09-06 교역자 회의 결과를 현재 구조와 대조했다. 항목별 판정·근거·교역자 회신 요청서는
[`docs/meetings/2026-09-06-교역자회의-결과-검토.md`](../docs/meetings/2026-09-06-교역자회의-결과-검토.md).

**이미 반영돼 있던 것 (변경 없음)**

| 회의 결과 | 현재 상태 |
|---|---|
| `새가족` 대메뉴 신설 · `목양`에서 새가족 제거 | 2026-08-23 승계 때 완료. 교차링크 한 줄도 없음 |
| `목양`의 `소모임` 계층 제거 → 구역모임·전도회·동호회 직접 노출 | 중간 계층 없음. 아래 집사회·권사회가 빠지면 회의안과 완전 일치 |
| 청년회 = `교육` | `/education#young-adult` |

**확정 대기 — 회신 전 착수 금지**

> DECISION NEEDED: **대메뉴 나열 순서.** 회의 결과가 `교회소개 / 새가족 / 예배와교육 / 목양과사역 / 교회활동`로
> **새가족을 2번째**에 적었다. 현재는 **맨 뒤 + highlight**이고 이것은 2026-08-12 디자이너 1:1 합의로 잠근 결정이다
> (모바일 맨 아래 유지 포함). **배치 순서 지시인지 단순 나열인지 확인**이 필요하다 — 순서 지시라면 8/12 잠금이 풀린다.
> 작업은 `NAV` 배열 순서 한 줄.

> DECISION NEEDED: **집사회·권사회 → `교회소개 > 섬기는 사람들`** (담임목사 확인 필요).
> 참고로 제시된 사랑의교회 페이지를 2026-09-06 확인한 결과 **개인 명단·사진 없이 "시무권사 2,188명"처럼
> 인원 수만 표기하는 조직 소개형**이었다 — 즉 **개인정보 이슈 없이 이동 가능**하다(집사 192명·권사 470명).
> 2026-08-23에 `목양`으로 흡수한 근거("전도회와 같은 조직 소개 템플릿")와, 회의안의 "직분 조직 vs 활동 조직"
> 구분 모두 성립한다. 확정 시 `/care#deacons` `/care#kwonsa` → `/intro#deacons` `/intro#kwonsa`.
> **함께 물을 것**: 집사·권사가 섬기는 사람들로 가면 남/여전도회가 `목양`에 남는 기준은 무엇인가.

> DECISION NEEDED: **`문화` 항목 채택 여부와 위치** (담임목사 확인 필요).
> **"대메뉴 5개 고정·신설 금지"(2026-08-23)는 기술 제약이 아니라 사용자 지시이며 철회 가능하다.**
> 코드 검토 결과 6번째 대메뉴는 **추가 개발 비용 없이 들어간다** — 헤더는 `flex gap-8` + max-w 1200이라 여유가 있고,
> `GROUP_COLS`가 4열까지 정의돼 있어 회의안 4그룹(취미·찬양·행사·시설)과 그대로 맞는다.
> 문제는 개수가 아니라 **내용물**이다: 목차 14개 중 **8개는 현행 사이트에 원고·사진이 전무**하고,
> 5개(동호회·시니어스쿨·교구친선리그·만나의집 카페·키즈그라운드)는 **이미 다른 메뉴에 배치돼 있다**.
> 또 분류 축이 "무엇을 하는가"에서 "어떤 성격인가"로 섞이고, 같은 회의의 `새가족` 강조 강화와 방향이 반대이며,
> 대메뉴 → 하위 **강등은 주소가 바뀌어 링크가 깨진다**(승격은 한 줄).
> **TF 권고 = 콘텐츠가 모인 뒤 승격.** 그 전 자리는 A(`교회 활동` 안 그룹) / B(GNB 밖 `/culture` 랜딩) 중 택일.

> DECISION NEEDED: **청년회를 `목양`에도 중복 게재하는가** (9/20 확정 예정).
> **TF는 반대.** 새가족 통합 때 세운 "교차링크 한 줄도 남기지 않아 도달 경로를 하나로" 원칙과 충돌하고
> 콘텐츠를 두 곳에서 관리하게 된다. **대안: GNB가 아니라 `/care` 페이지 안 카드 링크 한 개.**

**'문화' 비공개 운영 (TF 문의사항 답변)** — 가능하다. 세 단계로 비용이 다르다.
① **메뉴에서만 감추기**(주소를 아는 사람만 열람 + 검색 차단) = `lib/nav.ts` 플래그 + `noindex`, 반나절.
② **로그인 인증 비공개** = Supabase 미연결이라 **1차 오픈 범위 밖**.
③ **오픈** = 플래그 한 줄 삭제, 주소 그대로라 링크 안 깨짐.
→ **①을 권고.** 단 감춘 페이지도 원고는 만들어야 열리고, **오픈 기준(항목당 글자 수·사진 장수)을 지금 정해두지 않으면
영원히 열리지 않는다.** 항목 구성이 미확정이면 만들어놓고 감추는 것보다 **아직 안 만드는 쪽이 되돌리기 쉽다.**

## 페이지별 IA (스크롤 순서)

### 1. 메인 `/` (`context/pages/01-main.md`)

1. **헤로 영상** — 교회전경+햇빛 영상, 환영 카피 + 주일예배 시간
2. **이번 주일 예배 안내** — 시간/장소/설교제목/말씀본문
3. **설교 다시보기** — 최근 3개 영상 썸네일
4. **생방송 안내** — 다음 생방송 + 진입 버튼
5. **Go & Grow 캠페인 배너** — 인라인 또는 우하단 닫기 가능 카드
6. **새가족 환영** — 등록 CTA + 첫 방문 안내
7. **오시는 길** + 푸터

### 2. 교회 소개 `/intro` (`context/pages/02-intro.md`)

1. 헤로 — "경향교회 50주년" + 50주년 영상 큰 임베드
2. 인사말 (신승욱 담임)
3. 비전 / 신학 노선
4. 역사 (1973 → 2023 타임라인)
5. 섬기는 사람들 (실루엣 placeholder)
6. 교회 정보 (지도·시설·연락처)
7. (Q&A AI 자리만 — 1차 오픈 범위 외, 폼만 비활성)

### 3. 예배와 교육 `/worship` (`context/pages/03-worship.md`)

1. 헤로 — "예배와 교육"
2. 예배 및 모임 안내 `#times` — 표 3종 (예배·모임 / 주일학교 / S.F.C.)
3. 생방송 `#live` — 실시간 중계 + **예배 실황 다시보기** (분류 6종)
4. 특별순서 `#special` — 특송 · 간증 (분류 3종)
5. 교육 7종은 `/education` 라우트

> **2026-09-15 확정 (디자인팀 시안 + 사용자 확인).**
> - GNB `예배` 그룹이 4줄(예배 시간 안내·생방송·예배 실황·특별순서)에서 **3줄**이 됐다.
>   시안의 `생방송` 탭 화면이 곧 예배 실황 영상 화면이라, 위 2번 항목대로 **생방송 = 예배 실황**으로 합쳤다.
>   전체 아카이브(검색·페이지네이션)는 `/worship/live` 라우트가 계속 맡되 **GNB에는 노출하지 않고**
>   섹션 안 "전체 보기"로 들어간다.
> - 라벨 `예배 시간 안내` → **`예배 및 모임 안내`** (시안 탭 이름에 맞춤).
> - 이 세 항목이 그대로 `SubPage`의 앵커 탭 3개다 — 시안의 탭 3개와 1:1.
> - 되돌리기는 `lib/nav.ts`의 해당 3줄 수정으로 끝난다.

### 4. 목양과 사역 `/care` (`context/pages/04-care.md`)

**목양** (앵커 섹션 — 전부 `/care` 자기 앵커)
1. 헤로 — "함께 자라고 함께 섬깁니다"
2. 구역모임 `#district`
3. 전도회 `#evangelism` — 남전도회 / 여전도회 **섹션 안 탭**
4. 집사회 `#deacons` · 권사회 `#kwonsa` (2026-08-23 흡수)
5. 동호회 `#clubs`

**사역** (바로가기 카드 6 → `/ministry` 앵커)
별들의학교 · 제네바신학대학원 · 경향선교회 · 경향복지재단 · 경향학원 · 어린이집/놀이학원

> **새가족 영역은 `새가족` 대메뉴로 통합했다 (2026-08-23).** PPT 슬라이드 6의 `목양 > 새가족`
> (새가족등록·새가족모임)은 여기 두지 않는다 — 교차링크 한 줄도 남기지 않아 도달 경로를 하나로 만든다.
> `새가족모임`은 `새가족 > 등록하신 후 > 새가족 모임 (4주)`에, `새가족등록`은 온라인 유입 제외 결정으로 없다.
> 시니어스쿨·평생교육원은 2차 목차에서 `예배와 교육 > 교육`(`/education`)으로 이동했다.

### 5. 교회 활동 `/activity` (`context/pages/05-activity.md`)

허브 페이지 — 앵커 섹션은 캘린더 하나, 나머지는 독립 라우트로 나가는 바로가기 카드다.

1. 헤로 — "이번 주 경향교회"
2. **캘린더** `#calendar` (월/주 토글) — 유일한 앵커 섹션
3. 일정 — 경향의 일주일 `/activity/weekly`
4. 소식·자료 — 주보 · 영상뉴스 · 교회소식 · 교우소식 · 교단소식
5. **행정·신청** (현행 e교회행정 해체분) — 공지사항 · 자료실 · 신청·서식 · 시설 예약

> **결정(2026-07-01): 게시판형(수년치 누적·페이지네이션 필요) 하위 메뉴는 `/activity` 앵커섹션이 아니라
> 독립 라우트(`/activity/<slug>`)로 분리한다.** `/activity` 자체는 캘린더 + 각 게시판형 항목의
> 미리보기(이번 주 주보 카드, 최근 소식 N개)와 "전체보기" 링크만 갖는 허브 페이지가 된다.
>
> **사유**: `주보`(`bulletins` 테이블)·`교회소식/교우소식/교단소식`(`posts` 테이블, `category`로 구분)이
> 전부 "몇 년치 누적 → 페이지네이션" 구조로 설계돼 있음(`context/features/bulletin-pdf.md` "지난 주보 목록 —
> 페이지네이션(20개씩)"). 이걸 지금의 `SubPage`/`AnchorNav` 앵커-스크롤 패턴(한 페이지 안에 섹션 나열) 위에
> 그대로 얹으면 페이지가 지나치게 길어지고, 여러 목록의 페이지네이션 상태가 한 페이지에서 얽히고, 과거
> 게시물에 대한 개별 링크·SEO가 안 됨. 어드민 쪽(`/admin/bulletins`, `/admin/posts`, `context/features/admin-ui.md`
> locked)도 이미 독립 목록 라우트라 대칭적.
>
> **적용 방법**: `lib/nav.ts`의 해당 하위 메뉴 href를 `/activity#bulletin` 같은 해시 앵커에서 실제 라우트
> (`/activity/bulletin`)로 교체. 캘린더(교회 일정)는 이미 별도 UI 성격(`interactive/calendar.md`)이라 앵커
> 섹션이 아니라 `/activity` 안의 전용 위젯으로 유지(별도 라우트 불필요).
>
> ~~`> DECISION NEEDED:` **정확히 어떤 하위 메뉴가 여기 해당하는지**~~ → **2026-08-23 해소.**
> 게시판형 독립 라우트 = 주보 · 공지사항 · 교회소식 · 교우소식 · 교단소식 · 자료실.
> 영상뉴스 · 경향의 일주일은 영상 아카이브 성격이지만 누적량이 같아 **동일하게 독립 라우트**로 두고
> 내부 탭으로 분기한다(교구친선리그가 영상뉴스 탭으로 합류). 위 § 현행 사이트 잔여 메뉴 이관 참조.
> `context/pages/05-activity.md` 착수 가능.

### 6. 새가족 `/newcomer` (`context/pages/06-newcomer.md`)

1. 헤로 — "처음 오셨나요?"
2. 첫 방문 안내 (시간·장소·동선)
3. 연령별 기관 소개
4. **새가족 온라인 등록 폼** (`context/components/interactive/newcomer-form.md`)
5. 새가족 모임 안내
6. 학습·세례 안내

### 7. 어드민 `/admin/*`

> DECISION NEEDED: Supabase Studio 그대로 사용 (옵션 A) vs 자체 어드민 (옵션 B, +2~3주). `context/features/admin-ui.md` 참조.

## 공통 푸터 (전 페이지 동일)

- 좌상: 로고 + 한 줄 슬로건
- 1열: 주소 / 전화 / 이메일 / 오시는 길 링크
- 2열: 메인 메뉴 5개 링크
- 3열: 온라인 헌금 / e-행정 / 주보 링크
- 4열: SNS (유튜브·인스타·페이스북)
- 하단: 저작권 + 개인정보처리방침 + 이용약관

(`context/components/layout/footer.md`)

## 메타데이터 정책

각 페이지 `generateMetadata`:

| 페이지 | title | description |
|---|---|---|
| `/` | 경향교회 — 환영합니다 | 새신자·3040 친화·50주년… |
| `/intro` | 경향교회 소개 \| 1973년부터 | 50주년 교회의 비전과 역사… |
| `/worship` | 예배와 교육 \| 경향교회 | 주일예배·생방송·교육과정… |
| `/care` | 목양과 사역 \| 경향교회 | 새가족·구역모임·시니어스쿨… |
| `/activity` | 교회 일정과 소식 \| 경향교회 | 이번 달 일정·주보… |
| `/newcomer` | 처음 오신 분께 \| 경향교회 | 새가족 등록… |

OG 이미지·검색 노출 키워드는 `context/05-content-inventory.md` 카피 톤 가이드 참조.

## 새신자 진입 동선 보증

- 메인에서 "주일예배 시간"이 첫 화면(스크롤 없이)에 보여야 함
- 메인 → "오시는 길" 1뎁스 진입
- 모든 페이지의 GNB에 "새가족" 시각 강조
- ~~모바일 햄버거 메뉴 최상단에 "새가족" 배치~~ → **철회 (2026-08-12).** 메뉴 순서상 맨 아래 유지하되
  highlight 색으로 구분. 보완책은 위 "메인 자주 찾는 메뉴 아이콘" 검토 항목

(`guardrails/02-design-consistency.md` 체크리스트 항목)
